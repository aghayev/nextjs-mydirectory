

let formattedUrl = '/c/info/hubble-contacts-equivalent'
let pathname = '/info/hubble-contacts-equivalent'

const aa = formattedUrl.split('/').slice(2).join('/')

console.info(aa)

console.info(pathname.substring(1))
